[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.api"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

[env]
WEB_ORIGIN = "https://www.veraio.nl,https://besliswijzerweb-production.up.railway.app"
