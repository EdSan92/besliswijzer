services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: decision
      POSTGRES_PASSWORD: decision
      POSTGRES_DB: besliswijzer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U decision -d besliswijzer"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
